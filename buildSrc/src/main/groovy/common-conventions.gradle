plugins {
    id 'checkstyle'
}

repositories {
    mavenCentral()
}

checkstyle {
    toolVersion = '10.17.0'
    configFile = file("${rootDir}/config/checkstyle/intellij-java-google-style.xml")
    ignoreFailures = false
    maxWarnings = 0
}